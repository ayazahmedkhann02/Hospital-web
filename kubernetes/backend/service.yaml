apiVersion: v1
kind: Service
metadata:
  name: hospital-backend-service
  namespace: hospital-app
spec:
  selector:
    app: hospital-backend
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
  type: ClusterIP
