apiVersion: v1
kind: Service
metadata:
  name: hospital-frontend-service
  namespace: hospital-app
spec:
  selector:
    app: hospital-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
